// Code generated by SQLBoiler 4.8.6 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package mdbmodels

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// ContentUnitI18n is an object representing the database table.
type ContentUnitI18n struct {
	ContentUnitID    int64       `boil:"content_unit_id" json:"content_unit_id" toml:"content_unit_id" yaml:"content_unit_id"`
	Language         string      `boil:"language" json:"language" toml:"language" yaml:"language"`
	OriginalLanguage null.String `boil:"original_language" json:"original_language,omitempty" toml:"original_language" yaml:"original_language,omitempty"`
	Name             null.String `boil:"name" json:"name,omitempty" toml:"name" yaml:"name,omitempty"`
	Description      null.String `boil:"description" json:"description,omitempty" toml:"description" yaml:"description,omitempty"`
	UserID           null.Int64  `boil:"user_id" json:"user_id,omitempty" toml:"user_id" yaml:"user_id,omitempty"`
	CreatedAt        time.Time   `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *contentUnitI18nR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L contentUnitI18nL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ContentUnitI18nColumns = struct {
	ContentUnitID    string
	Language         string
	OriginalLanguage string
	Name             string
	Description      string
	UserID           string
	CreatedAt        string
}{
	ContentUnitID:    "content_unit_id",
	Language:         "language",
	OriginalLanguage: "original_language",
	Name:             "name",
	Description:      "description",
	UserID:           "user_id",
	CreatedAt:        "created_at",
}

var ContentUnitI18nTableColumns = struct {
	ContentUnitID    string
	Language         string
	OriginalLanguage string
	Name             string
	Description      string
	UserID           string
	CreatedAt        string
}{
	ContentUnitID:    "content_unit_i18n.content_unit_id",
	Language:         "content_unit_i18n.language",
	OriginalLanguage: "content_unit_i18n.original_language",
	Name:             "content_unit_i18n.name",
	Description:      "content_unit_i18n.description",
	UserID:           "content_unit_i18n.user_id",
	CreatedAt:        "content_unit_i18n.created_at",
}

// Generated where

var ContentUnitI18nWhere = struct {
	ContentUnitID    whereHelperint64
	Language         whereHelperstring
	OriginalLanguage whereHelpernull_String
	Name             whereHelpernull_String
	Description      whereHelpernull_String
	UserID           whereHelpernull_Int64
	CreatedAt        whereHelpertime_Time
}{
	ContentUnitID:    whereHelperint64{field: "\"content_unit_i18n\".\"content_unit_id\""},
	Language:         whereHelperstring{field: "\"content_unit_i18n\".\"language\""},
	OriginalLanguage: whereHelpernull_String{field: "\"content_unit_i18n\".\"original_language\""},
	Name:             whereHelpernull_String{field: "\"content_unit_i18n\".\"name\""},
	Description:      whereHelpernull_String{field: "\"content_unit_i18n\".\"description\""},
	UserID:           whereHelpernull_Int64{field: "\"content_unit_i18n\".\"user_id\""},
	CreatedAt:        whereHelpertime_Time{field: "\"content_unit_i18n\".\"created_at\""},
}

// ContentUnitI18nRels is where relationship names are stored.
var ContentUnitI18nRels = struct {
	ContentUnit string
	User        string
}{
	ContentUnit: "ContentUnit",
	User:        "User",
}

// contentUnitI18nR is where relationships are stored.
type contentUnitI18nR struct {
	ContentUnit *ContentUnit `boil:"ContentUnit" json:"ContentUnit" toml:"ContentUnit" yaml:"ContentUnit"`
	User        *User        `boil:"User" json:"User" toml:"User" yaml:"User"`
}

// NewStruct creates a new relationship struct
func (*contentUnitI18nR) NewStruct() *contentUnitI18nR {
	return &contentUnitI18nR{}
}

// contentUnitI18nL is where Load methods for each relationship are stored.
type contentUnitI18nL struct{}

var (
	contentUnitI18nAllColumns            = []string{"content_unit_id", "language", "original_language", "name", "description", "user_id", "created_at"}
	contentUnitI18nColumnsWithoutDefault = []string{"content_unit_id", "language"}
	contentUnitI18nColumnsWithDefault    = []string{"original_language", "name", "description", "user_id", "created_at"}
	contentUnitI18nPrimaryKeyColumns     = []string{"content_unit_id", "language"}
	contentUnitI18nGeneratedColumns      = []string{}
)

type (
	// ContentUnitI18nSlice is an alias for a slice of pointers to ContentUnitI18n.
	// This should almost always be used instead of []ContentUnitI18n.
	ContentUnitI18nSlice []*ContentUnitI18n

	contentUnitI18nQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	contentUnitI18nType                 = reflect.TypeOf(&ContentUnitI18n{})
	contentUnitI18nMapping              = queries.MakeStructMapping(contentUnitI18nType)
	contentUnitI18nPrimaryKeyMapping, _ = queries.BindMapping(contentUnitI18nType, contentUnitI18nMapping, contentUnitI18nPrimaryKeyColumns)
	contentUnitI18nInsertCacheMut       sync.RWMutex
	contentUnitI18nInsertCache          = make(map[string]insertCache)
	contentUnitI18nUpdateCacheMut       sync.RWMutex
	contentUnitI18nUpdateCache          = make(map[string]updateCache)
	contentUnitI18nUpsertCacheMut       sync.RWMutex
	contentUnitI18nUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single contentUnitI18n record from the query.
func (q contentUnitI18nQuery) One(exec boil.Executor) (*ContentUnitI18n, error) {
	o := &ContentUnitI18n{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "mdbmodels: failed to execute a one query for content_unit_i18n")
	}

	return o, nil
}

// All returns all ContentUnitI18n records from the query.
func (q contentUnitI18nQuery) All(exec boil.Executor) (ContentUnitI18nSlice, error) {
	var o []*ContentUnitI18n

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "mdbmodels: failed to assign all query results to ContentUnitI18n slice")
	}

	return o, nil
}

// Count returns the count of all ContentUnitI18n records in the query.
func (q contentUnitI18nQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "mdbmodels: failed to count content_unit_i18n rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q contentUnitI18nQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "mdbmodels: failed to check if content_unit_i18n exists")
	}

	return count > 0, nil
}

// ContentUnit pointed to by the foreign key.
func (o *ContentUnitI18n) ContentUnit(mods ...qm.QueryMod) contentUnitQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ContentUnitID),
	}

	queryMods = append(queryMods, mods...)

	query := ContentUnits(queryMods...)
	queries.SetFrom(query.Query, "\"content_units\"")

	return query
}

// User pointed to by the foreign key.
func (o *ContentUnitI18n) User(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.UserID),
	}

	queryMods = append(queryMods, mods...)

	query := Users(queryMods...)
	queries.SetFrom(query.Query, "\"users\"")

	return query
}

// LoadContentUnit allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (contentUnitI18nL) LoadContentUnit(e boil.Executor, singular bool, maybeContentUnitI18n interface{}, mods queries.Applicator) error {
	var slice []*ContentUnitI18n
	var object *ContentUnitI18n

	if singular {
		object = maybeContentUnitI18n.(*ContentUnitI18n)
	} else {
		slice = *maybeContentUnitI18n.(*[]*ContentUnitI18n)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &contentUnitI18nR{}
		}
		args = append(args, object.ContentUnitID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &contentUnitI18nR{}
			}

			for _, a := range args {
				if a == obj.ContentUnitID {
					continue Outer
				}
			}

			args = append(args, obj.ContentUnitID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`content_units`),
		qm.WhereIn(`content_units.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ContentUnit")
	}

	var resultSlice []*ContentUnit
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ContentUnit")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for content_units")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for content_units")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.ContentUnit = foreign
		if foreign.R == nil {
			foreign.R = &contentUnitR{}
		}
		foreign.R.ContentUnitI18ns = append(foreign.R.ContentUnitI18ns, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ContentUnitID == foreign.ID {
				local.R.ContentUnit = foreign
				if foreign.R == nil {
					foreign.R = &contentUnitR{}
				}
				foreign.R.ContentUnitI18ns = append(foreign.R.ContentUnitI18ns, local)
				break
			}
		}
	}

	return nil
}

// LoadUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (contentUnitI18nL) LoadUser(e boil.Executor, singular bool, maybeContentUnitI18n interface{}, mods queries.Applicator) error {
	var slice []*ContentUnitI18n
	var object *ContentUnitI18n

	if singular {
		object = maybeContentUnitI18n.(*ContentUnitI18n)
	} else {
		slice = *maybeContentUnitI18n.(*[]*ContentUnitI18n)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &contentUnitI18nR{}
		}
		if !queries.IsNil(object.UserID) {
			args = append(args, object.UserID)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &contentUnitI18nR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.UserID) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.UserID) {
				args = append(args, obj.UserID)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.User = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.ContentUnitI18ns = append(foreign.R.ContentUnitI18ns, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.UserID, foreign.ID) {
				local.R.User = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.ContentUnitI18ns = append(foreign.R.ContentUnitI18ns, local)
				break
			}
		}
	}

	return nil
}

// SetContentUnit of the contentUnitI18n to the related item.
// Sets o.R.ContentUnit to related.
// Adds o to related.R.ContentUnitI18ns.
func (o *ContentUnitI18n) SetContentUnit(exec boil.Executor, insert bool, related *ContentUnit) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"content_unit_i18n\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"content_unit_id"}),
		strmangle.WhereClause("\"", "\"", 2, contentUnitI18nPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ContentUnitID, o.Language}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ContentUnitID = related.ID
	if o.R == nil {
		o.R = &contentUnitI18nR{
			ContentUnit: related,
		}
	} else {
		o.R.ContentUnit = related
	}

	if related.R == nil {
		related.R = &contentUnitR{
			ContentUnitI18ns: ContentUnitI18nSlice{o},
		}
	} else {
		related.R.ContentUnitI18ns = append(related.R.ContentUnitI18ns, o)
	}

	return nil
}

// SetUser of the contentUnitI18n to the related item.
// Sets o.R.User to related.
// Adds o to related.R.ContentUnitI18ns.
func (o *ContentUnitI18n) SetUser(exec boil.Executor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"content_unit_i18n\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"user_id"}),
		strmangle.WhereClause("\"", "\"", 2, contentUnitI18nPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ContentUnitID, o.Language}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.UserID, related.ID)
	if o.R == nil {
		o.R = &contentUnitI18nR{
			User: related,
		}
	} else {
		o.R.User = related
	}

	if related.R == nil {
		related.R = &userR{
			ContentUnitI18ns: ContentUnitI18nSlice{o},
		}
	} else {
		related.R.ContentUnitI18ns = append(related.R.ContentUnitI18ns, o)
	}

	return nil
}

// RemoveUser relationship.
// Sets o.R.User to nil.
// Removes o from all passed in related items' relationships struct (Optional).
func (o *ContentUnitI18n) RemoveUser(exec boil.Executor, related *User) error {
	var err error

	queries.SetScanner(&o.UserID, nil)
	if _, err = o.Update(exec, boil.Whitelist("user_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.User = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.ContentUnitI18ns {
		if queries.Equal(o.UserID, ri.UserID) {
			continue
		}

		ln := len(related.R.ContentUnitI18ns)
		if ln > 1 && i < ln-1 {
			related.R.ContentUnitI18ns[i] = related.R.ContentUnitI18ns[ln-1]
		}
		related.R.ContentUnitI18ns = related.R.ContentUnitI18ns[:ln-1]
		break
	}
	return nil
}

// ContentUnitI18ns retrieves all the records using an executor.
func ContentUnitI18ns(mods ...qm.QueryMod) contentUnitI18nQuery {
	mods = append(mods, qm.From("\"content_unit_i18n\""))
	return contentUnitI18nQuery{NewQuery(mods...)}
}

// FindContentUnitI18n retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindContentUnitI18n(exec boil.Executor, contentUnitID int64, language string, selectCols ...string) (*ContentUnitI18n, error) {
	contentUnitI18nObj := &ContentUnitI18n{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"content_unit_i18n\" where \"content_unit_id\"=$1 AND \"language\"=$2", sel,
	)

	q := queries.Raw(query, contentUnitID, language)

	err := q.Bind(nil, exec, contentUnitI18nObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "mdbmodels: unable to select from content_unit_i18n")
	}

	return contentUnitI18nObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ContentUnitI18n) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("mdbmodels: no content_unit_i18n provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(contentUnitI18nColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	contentUnitI18nInsertCacheMut.RLock()
	cache, cached := contentUnitI18nInsertCache[key]
	contentUnitI18nInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			contentUnitI18nAllColumns,
			contentUnitI18nColumnsWithDefault,
			contentUnitI18nColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(contentUnitI18nType, contentUnitI18nMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(contentUnitI18nType, contentUnitI18nMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"content_unit_i18n\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"content_unit_i18n\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "mdbmodels: unable to insert into content_unit_i18n")
	}

	if !cached {
		contentUnitI18nInsertCacheMut.Lock()
		contentUnitI18nInsertCache[key] = cache
		contentUnitI18nInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the ContentUnitI18n.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ContentUnitI18n) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	contentUnitI18nUpdateCacheMut.RLock()
	cache, cached := contentUnitI18nUpdateCache[key]
	contentUnitI18nUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			contentUnitI18nAllColumns,
			contentUnitI18nPrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("mdbmodels: unable to update content_unit_i18n, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"content_unit_i18n\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, contentUnitI18nPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(contentUnitI18nType, contentUnitI18nMapping, append(wl, contentUnitI18nPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "mdbmodels: unable to update content_unit_i18n row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "mdbmodels: failed to get rows affected by update for content_unit_i18n")
	}

	if !cached {
		contentUnitI18nUpdateCacheMut.Lock()
		contentUnitI18nUpdateCache[key] = cache
		contentUnitI18nUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q contentUnitI18nQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "mdbmodels: unable to update all for content_unit_i18n")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "mdbmodels: unable to retrieve rows affected for content_unit_i18n")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ContentUnitI18nSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("mdbmodels: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), contentUnitI18nPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"content_unit_i18n\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, contentUnitI18nPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "mdbmodels: unable to update all in contentUnitI18n slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "mdbmodels: unable to retrieve rows affected all in update all contentUnitI18n")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ContentUnitI18n) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("mdbmodels: no content_unit_i18n provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(contentUnitI18nColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	contentUnitI18nUpsertCacheMut.RLock()
	cache, cached := contentUnitI18nUpsertCache[key]
	contentUnitI18nUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			contentUnitI18nAllColumns,
			contentUnitI18nColumnsWithDefault,
			contentUnitI18nColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			contentUnitI18nAllColumns,
			contentUnitI18nPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("mdbmodels: unable to upsert content_unit_i18n, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(contentUnitI18nPrimaryKeyColumns))
			copy(conflict, contentUnitI18nPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"content_unit_i18n\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(contentUnitI18nType, contentUnitI18nMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(contentUnitI18nType, contentUnitI18nMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "mdbmodels: unable to upsert content_unit_i18n")
	}

	if !cached {
		contentUnitI18nUpsertCacheMut.Lock()
		contentUnitI18nUpsertCache[key] = cache
		contentUnitI18nUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single ContentUnitI18n record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ContentUnitI18n) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("mdbmodels: no ContentUnitI18n provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), contentUnitI18nPrimaryKeyMapping)
	sql := "DELETE FROM \"content_unit_i18n\" WHERE \"content_unit_id\"=$1 AND \"language\"=$2"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "mdbmodels: unable to delete from content_unit_i18n")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "mdbmodels: failed to get rows affected by delete for content_unit_i18n")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q contentUnitI18nQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("mdbmodels: no contentUnitI18nQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "mdbmodels: unable to delete all from content_unit_i18n")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "mdbmodels: failed to get rows affected by deleteall for content_unit_i18n")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ContentUnitI18nSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), contentUnitI18nPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"content_unit_i18n\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, contentUnitI18nPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "mdbmodels: unable to delete all from contentUnitI18n slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "mdbmodels: failed to get rows affected by deleteall for content_unit_i18n")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ContentUnitI18n) Reload(exec boil.Executor) error {
	ret, err := FindContentUnitI18n(exec, o.ContentUnitID, o.Language)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ContentUnitI18nSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ContentUnitI18nSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), contentUnitI18nPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"content_unit_i18n\".* FROM \"content_unit_i18n\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, contentUnitI18nPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "mdbmodels: unable to reload all in ContentUnitI18nSlice")
	}

	*o = slice

	return nil
}

// ContentUnitI18nExists checks if the ContentUnitI18n row exists.
func ContentUnitI18nExists(exec boil.Executor, contentUnitID int64, language string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"content_unit_i18n\" where \"content_unit_id\"=$1 AND \"language\"=$2 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, contentUnitID, language)
	}
	row := exec.QueryRow(sql, contentUnitID, language)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "mdbmodels: unable to check if content_unit_i18n exists")
	}

	return exists, nil
}
